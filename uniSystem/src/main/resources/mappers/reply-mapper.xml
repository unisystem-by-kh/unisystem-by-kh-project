<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="replyMapper">

	<!-- 댓글 삽입 -->
	<insert id="insert">
		INSERT INTO "REPLY"
				VALUES(
					SEQ_REPLY_NO.NEXTVAL, 
					${replyContnet} , DEFAULT, DEFAULT, 
					${boardNo}, 
					${memberNo}
				)
	</insert>

</mapper>








